# Copier en .env (jamais commité) ou exporter ces variables sur le serveur
# de déploiement avant de lancer avec --spring.profiles.active=prod.
DB_URL=jdbc:postgresql://localhost:5432/club_informatique
DB_USERNAME=club_info
DB_PASSWORD=change-me
CORS_ALLOWED_ORIGINS=https://club-informatique.example.bf
