# Profil de développement local : Postgres lancé via docker-compose (voir README.md racine).
spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/club_informatique
    username: club_info
    password: club_info_dev
  jpa:
    show-sql: true

logging:
  level:
    bf.ist.clubinfo: DEBUG
    org.hibernate.SQL: DEBUG

app:
  cors:
    allowed-origins: http://localhost:4200
